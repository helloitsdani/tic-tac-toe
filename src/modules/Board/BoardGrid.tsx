import React, { FunctionComponent } from 'react'
import { ReactComponent as BoardIcon } from '../../assets/board.svg'

const BoardGrid: FunctionComponent = () => (
  <BoardIcon className="c-game-board__grid" />
)

export default BoardGrid
